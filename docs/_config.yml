title: Testing github pages
description: Screaming in the dark.
#show_downloads: true
google_analytics:
#remote_theme: poole/lanyon
#plugins:
#  - jekyll-paginate
#theme: jekyll-theme-minimal
defaults:
  -
    scope:
      path: "projects"
      type: "pages"
    values:
      layout: "project"
      author: "Mr. Hyde"
      category: "project"
